{
    "devDependencies": {
        "@types/node": "^16.11.9",
        "concurrently": "^6.4.0",
        "nodemon": "^2.0.15",
        "prettier": "^2.4.1",
        "typescript": "^4.5.2"
    },
    "scripts": {
        "dev:tsc": "tsc -w",
        "dev:nodemon": "nodemon dist/BarrierTs",
        "dev": "concurrently -r -k -i npm:dev:*",
        "start": "node dist/BarrierTs",
        "build": "tsc",
        "build:start": "tsc && node dist/BarrierTs",
        "prettier": "prettier src --config .prettierrc --write"
    }
}
